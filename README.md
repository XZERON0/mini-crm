# Mini CRM

> Для просмотра md (markdown) рекомендуется нажать на горяч. клавиши ```CTRL+SHIFT+V``` 

Для запуска проекта есть два способа:
1. с докер
    ```bash
    docker-compose up --build
    ```
2. В ручную (т.е. без Докер)
   ```bash
   php artisan serve
   ```

